# النشر على أي دومين (بدون ربط Vercel)

المشروع مبني بـ Next.js ويمكن تشغيله على أي مزوّد استضافة يدعم Node.js (مثل VPS أو Docker أو أي PaaS).

## الإعداد

1. ثبّت الاعتمادات:

```bash
pnpm install
```

2. ابنِ المشروع:

```bash
pnpm build
```

3. شغّل السيرفر:

```bash
pnpm start
```

> إذا كانت منصة الاستضافة تفرض منفذًا معيّنًا، عيّن `PORT` في بيئة التشغيل (Next.js يقرأه تلقائيًا).

## متغيرات البيئة

ضع المتغيرات التالية في بيئة السيرفر قبل التشغيل:

- `GOOGLE_GENERATIVE_AI_API_KEY`
- `MISTRAL_API_KEY`

## ربط دومين مخصص

- اربط الدومين أو الساب دومين مع IP/Endpoint الخاص بالاستضافة.
- وجّه الطلبات إلى التطبيق عبر Reverse Proxy (مثل Nginx أو Caddy).
- فعّل HTTPS (Let's Encrypt أو مزوّد الاستضافة).
